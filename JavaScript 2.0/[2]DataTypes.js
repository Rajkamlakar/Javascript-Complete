//primitives(use stack memory)(they gives u copy of the original value)=string,number,boolean,null,undefind,Symbol,BigInt

//Refarance/non-premitives(use heap memory)(they gives the directly refrance to the datatypes)=Arrys,Objects,Function

let name = "raj"; //string DataType
let age = 20; //number datatype
let islog = false; //boolean datatype
let x = null; //null datatype(null is a Object type)
let r; //undefind datatype(undefind is a undefind type)
//this all are the primitive  Datatype

//another datatype is=> Object

let a = 2 ** 4;
console.log(a);

console.log(typeof null);
console.log(typeof age);
console.log(typeof name);
